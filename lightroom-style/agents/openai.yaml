interface:
  display_name: "Lightroom Style"
  short_description: "Match reference colors through native Lightroom edits"
  default_prompt: "Use $lightroom-style to analyze my reference photos and grade the target in local Lightroom, preserving the original and verifying the native render without generative editing."
